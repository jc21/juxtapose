<div class="modal-dialog">
    <div class="modal-content">
        <form class="form-horizontal">
            <div class="modal-header text-left">
                <h4 class="modal-title">Template Options</h4>
            </div>
            <div class="modal-body">
                <%
                var template = getTemplate();
                if (template.default_options && template.default_options.length) { %>
                    <p class="text-center">You can optionally customise your notification.</p>
                    <%
                    _.map(template.default_options, function (val, key) {
                        %>
                        <div class="form-group">
                            <label class="col-sm-3 control-label"><%- niceVarName(key) %></label>
                            <div class="col-sm-9">
                                <input type="text" class="form-control" placeholder="<%- val %>" name="<%- key %>" value="">
                            </div>
                        </div>
                        <%
                    });
                } else {
                    %>
                    <p class="text-center">There are no customisable options for this Service.</p>
                    <%
                }
                %>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-accent save">Save</button>
            </div>
        </form>
    </div>
</div>
